
  # Build MediTimeline Application

  This is a code bundle for Build MediTimeline Application. The original project is available at https://www.figma.com/design/sz5d9LA1Y5Bd3H89nO2HwV/Build-MediTimeline-Application.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  